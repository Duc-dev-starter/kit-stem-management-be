const Combo = require('./Combo');
const validateCreateCombo = require('./CreateComboSchema');
const validateSearchCombo = require('./SearchComboSchema');

module.exports = {
    Combo,
    validateCreateCombo,
    validateSearchCombo
}